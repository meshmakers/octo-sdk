steps:
- task: Assembly-Info-NetCore@2
  displayName: "Versioning (no nuget)"
  inputs:
    FileNames: |
      **/*.csproj
      !src/Frontend/Client/*.csproj
      !src/Common/Shared/*.csproj      
      !src/Backend/Common/*.csproj
    InsertAttributes: true
    FileEncoding: 'auto'
    GeneratePackageOnBuild: false
    Authors: Meshmakers GmbH and contributor
    Company: $(Company)
    Product: $(Build.SourceBranchName) - $(Build.SourceVersion) (release)
    Copyright: $(Copyright)
    PackageProjectUrl: https://www.meshmakers.io
    RepositoryUrl: https://github.com/meshmakers/
    PackageTags: Octo data mesh iot
    VersionNumber: $(MajorVersion).$(MinorVersion).0.0
    FileVersionNumber: $(Build.BuildNumber)
    InformationalVersion: $(Build.BuildNumber)
    PackageVersion: $(Build.BuildNumber)
- task: Assembly-Info-NetCore@2
  displayName: "Versioning (nuget)"
  inputs:
    FileNames: |      
      src/Frontend/Client/*.csproj
      src/Common/Shared/*.csproj
      src/Backend/Common/*.csproj
    InsertAttributes: true
    FileEncoding: 'auto'
    GeneratePackageOnBuild: true
    Authors: Meshmakers GmbH and contributors
    Company: $(Company)
    Product: $(Build.SourceBranchName) - $(Build.SourceVersion) (release)
    Copyright: $(Copyright)
    PackageProjectUrl: https://www.meshmakers.io
    RepositoryUrl: https://github.com/meshmakers/
    PackageTags: Octo data mesh iot
    VersionNumber: $(MajorVersion).$(MinorVersion).0.0
    FileVersionNumber: $(Build.BuildNumber)
    InformationalVersion: $(Build.BuildNumber)
    PackageVersion: $(Build.BuildNumber)    
- task: VersionJSONFile@2
  displayName: Set Version package.json
  inputs:
    versionForJSONFileFormat: '{1}.{2}.{3}-{4}'
    FilenamePattern: ^package.json
