#parameters:
#  - name: ContainerRegistry
#    type: string
#  - name: Repository
#    type: string
#  - name: Dockerfile
#    type: string
#  - name: IsRelease
#    type: boolean

steps:
  - task: DotNetCoreCLI@2
    displayName: 'Install xmldocmd as a dotnet tool'
    inputs:
      command: custom
      custom: tool
      arguments: 'install --global xmldocmd'
  - task: PowerShell@2
    inputs:
      targetType: 'inline'
      script: |
        $output = "$(build.artifactstagingdirectory)/documentation"
        $path = "$(Build.SourcesDirectory)/bin/$(BuildConfiguration)/Shared/Frontend/Meshmakers.Octo.Frontend.Client.dll"
        Write-Host "Creating documentation for $path, doc is generated at $output"
        xmldocmd $path test
    displayName: 'create documentation'

        
