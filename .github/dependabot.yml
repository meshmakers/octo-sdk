# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
  - package-ecosystem: "nuget" # See documentation for possible values
    directory: "/" # Location of package manifests (root solution covers src, tests and samples)
    schedule:
      interval: "weekly"
    ignore:
      # Compiler-bound: Microsoft.CodeAnalysis.* stays at the Roslyn version of the pinned .NET SDK (Sdk.Common.Web packs the compile closure as analyzers; a newer Roslyn breaks every consumer with CS9057).
      - dependency-name: "Microsoft.CodeAnalysis.*"
